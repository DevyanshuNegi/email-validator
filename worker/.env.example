# Local Dev Mode Configuration
# Set to true for local development with MailHog, false for production
IS_DEV=true

# Database Configuration
# PostgreSQL connection string
# Format: postgres://user:password@host:port/database?sslmode=disable
DATABASE_URL=postgres://postgres:postgres@localhost:5433/emailvalidator?sslmode=disable

# Redis Configuration
REDIS_ADDR=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0

# Production Safety Configuration
# Worker hostname for SMTP HELO/EHLO command
# Should be a valid hostname (not localhost)
WORKER_HOSTNAME=worker1.yourdomain.com

# SOCKS5 Proxy Configuration (Required for Production)
# Uncomment and configure for production use:
# SOCKS5_PROXY=123.45.67.89:1080
# PROXY_USER=your_username
# PROXY_PASS=your_password
